def sort_list(numbers):
    """
    This function takes a list of numbers and returns a new list sorted in ascending order.

    Parameters:
    numbers (list): A list of numbers (integers or floats).

    Returns:
    list: A new list containing the sorted numbers.
    """
    return sorted(numbers)

# Example usage
unsorted_list = [5, 2, 9, 1, 5, 6]
sorted_list = sort_list(unsorted_list)
print(sorted_list)  # Output: [1, 2, 5, 5, 6, 9]
